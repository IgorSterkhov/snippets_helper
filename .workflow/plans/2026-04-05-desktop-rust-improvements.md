# Keyboard Helper Rust — План доработок

## 1. Иконка приложения
- Сгенерировать 10 вариантов иконки
- Презентовать через веб-превью для выбора
- Заменить заглушку (синий квадрат) на выбранную иконку
- Форматы: PNG (32, 128, 256), ICO (Windows), ICNS (macOS)

## 2. Markdown preview в Notes
- Текущий: базовый рендеринг (h1-h3, bold, code, lists)
- Улучшить: подключить полноценную JS-библиотеку marked.js (bundled)
- Поддержка: таблицы, вложенные списки, ссылки, изображения, code blocks с подсветкой

## 3. Размер шрифта в настройках
- Проверить что настройка `font_size` из Settings → General применяется ко всем вкладкам
- Сейчас: возможно читается только на Shortcuts, остальные вкладки игнорируют
- Исправить: все вкладки должны подхватывать глобальный font_size

## 4. Модальность окна (always on top)
- Добавить в Settings → General переключатель "Always on top" (да/нет)
- Сейчас: `alwaysOnTop: true` захардкожен в tauri.conf.json
- Доработка: читать из app_settings, применять через Tauri Window API
- По умолчанию: true (как сейчас)

## 5. Синхронизация snippet tags через API
- Таблица `snippet_tags` уже добавлена в `SYNCED_TABLES` и `sync/schema.rs` на клиенте
- Нужно: добавить модель `SnippetTag` в API сервер (`api/models.py`)
- Alembic миграция: создать таблицу `snippet_tags` в PostgreSQL
- Добавить в `TABLE_MODELS` для sync routes
- Обновить Python `shared/sync_schema.py` — добавить `snippet_tags`
- После этого теги будут автоматически синхронизироваться между устройствами

## 6. Интеграция с Obsidian
- Требует детальной проработки (brainstorming)
- Цель: сниппеты могут создавать, открывать и редактировать заметки в Obsidian
- Нужно определить: механизм взаимодействия, формат данных, UI

## 6. Вкладка VPS Management
- Требует детальной проработки (brainstorming)
- Новая вкладка в приложении для управления VPS серверами

### 6.1 Мониторинг VPS
- Подключение по SSH к серверу
- Показывать: CPU, RAM, Disk (занято/свободно)
- Список запущенных сервисов (не системных)
- Просмотр логов сервисов

### 6.2 SSH сертификаты
- Установка SSH-ключа с локальной машины на VPS (аналог ssh-copy-id)
- Генерация нового ключа с суффиксом для идентификации машины
- Выбор из существующих ключей (~/.ssh/)

### 6.3 SSH алиасы
- Создание алиаса для SSH-подключения к серверу
- UI для выбора параметров SSH с описаниями (-L port forwarding, -X X11 forwarding и т.д.)
- Придумать имя алиаса
- Выбор файла для сохранения (файловый менеджер или ручной ввод пути)
- Путь к файлу алиасов сохраняется в настройках приложения
