// Help content for the Tasks tab. Rendered inside the shared SQL help modal
// (`sql-help.js`) via the `?` button in the tab header.

export const TASKS_HELP_HTML = `
<p><strong>Tasks</strong> — личный task-менеджер с иерархическими чекбоксами,
категориями, статусами и ссылками на внешние трекеры (YouTrack/Jira/Linear).
Данные хранятся локально в SQLite и синхронизируются с удалённым API вместе
с остальными модулями.</p>

<h4>Устройство</h4>
<ul>
  <li><strong>Chip strip сверху</strong> — все запиненные задачи. Клик по чипу
      открывает задачу в развёрнутом виде.</li>
  <li><strong>Фильтры</strong> — два dropdown для Category и Status.
      Single-select, «All» сбрасывает, «None» появляется если есть задачи
      без категории/статуса (после удаления). Правый клик на dropdown —
      открыть модалку управления (добавление/удаление/порядок/цвет).</li>
  <li><strong>Layout toggle</strong> (правый верхний угол) — переключение
      одноколоночного и двухколоночного вида. Порядок карточек row-major
      (zigzag). Выбор сохраняется между запусками.</li>
  <li><strong>Карточки</strong> — две формы:
      <em>collapsed</em> показывает название, бейджи Category/Status,
      компактную кнопку-трекер 🎫 и список чекбоксов (со скроллом если
      много). <em>Expanded</em> открывается кнопкой ▼ или двойным кликом
      по чипу — полное редактирование всех полей.</li>
</ul>

<h4>Чекбоксы</h4>
<ul>
  <li>Вложенность до 3 уровней. Каждый уровень — отступ слева.</li>
  <li><strong>Enter</strong> — новый пункт на том же уровне.</li>
  <li><strong>Tab</strong> — сделать дочерним верхнего соседа.</li>
  <li><strong>Shift+Tab</strong> — поднять на уровень выше.</li>
  <li><strong>⋮⋮</strong> слева — DnD-рукоятка: перемещать по списку,
      менять родителя.</li>
  <li>Полупрозрачный «<em>+ Add item…</em>» в конце — добавить пункт.</li>
</ul>

<h4>Drag-and-drop</h4>
<ul>
  <li>Карточку за <strong>⋮⋮</strong> вверху можно:
      <ul>
        <li>перенести на <em>dropdown</em> Category/Status — после паузы
            dropdown раскроется, drop на пункт меняет category/status
            задачи (фильтр при этом не меняется);</li>
        <li>перенести на другую карточку — поменять порядок в списке.</li>
      </ul>
  </li>
  <li>Чекбокс за <strong>⋮⋮</strong> можно перенести в другое место внутри
      своей задачи; правила: глубина ≤ 3.</li>
</ul>

<h4>Ссылки</h4>
<p>
  <strong>Tracker link</strong> — одна, основная ссылка на внешнюю систему
  трекинга. В collapsed-виде есть компактная 🎫-кнопка. В expanded-виде —
  обычный text-input.
</p>
<p>
  <strong>Links</strong> — дополнительные ссылки (документация, обсуждения,
  PR-ы). Каждая: URL + опциональный label (если пусто — покажется домен).
  Reorder через ⋮⋮ слева от строки. В collapsed-виде не показываются.
</p>

<h4>Категории и статусы</h4>
<p>
  При первом запуске заданы по умолчанию:
  <em>Categories:</em> Work, Home; <em>Statuses:</em> Open, In progress,
  Blocked, Done.
</p>
<p>
  Правый клик на dropdown-е Category или Status → «⚙ Manage…» — модалка
  со списком, кнопками «+ Add», inline-rename, выбор цвета (палитра +
  «Custom…»), удаление и reorder через DnD.
</p>
<p>
  При удалении категории, если задачи её используют, задачи не удаляются —
  их <code>category_id</code> становится <code>NULL</code>. В dropdown-фильтре
  тогда появится пункт <em>None</em>, показывающий такие «осиротевшие»
  задачи. Пункт исчезает, когда всем задачам снова назначена категория.
</p>

<h4>Pin</h4>
<p>
  В expanded-виде — кнопка pin. Закреплённые задачи всегда вверху списка
  и попадают в chip strip наверху таба.
</p>

<h4>Notes</h4>
<p>
  Поле «Notes» в expanded-виде — Markdown с тулбаром (<strong>B</strong>,
  <strong>I</strong>, заголовки, списки, ссылки, изображения, код, таблицы).
  Видно только в expanded, в collapsed-виде не показывается для экономии
  места.
</p>

<h4>Цвет карточки</h4>
<p>
  В expanded-виде — палитра 6 пастельных фонов + «Custom» (нативный
  color-picker для произвольного hex). Накладывается поверх
  <code>--bg-secondary</code> как полупрозрачный тинт.
</p>

<h4>Синхронизация</h4>
<p>
  Все 5 таблиц (categories, statuses, tasks, checkboxes, links) участвуют
  в общем sync-флоу. Изменения помечаются как <code>pending</code> и
  улетают при следующем sync-е вместе с заметками и сниппетами.
</p>
`;
